
1.值类型：基本数类型（int系列），float类型，bool型，string型，数组，结构体struct
a.变量直接储存值，内存通常在栈中分配（栈区）

2.引用类型：指针，slice切片，管道，interface接口
a.变量存储的是一个地址，这个地址对应的空间才是真正的储存数据（值），内存通常在堆上分配，
当没有任何的变量引入这个地址时，该地址对应的空间成为一个垃圾，由GC来回收（堆区）